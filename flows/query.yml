!Flow
version: '1'
with:
  read_only: true
  port_expose: $JINA_PORT
pods:
  - name: encoder
    uses: pods/encode.yml
    timeout_ready: 1000000
    read_only: true
  - name: chunk_indexer
    uses: pods/index.yml
 
